# Base URL for QR code generation (e.g., https://your-domain.com)
BASE_URL=http://localhost

# WebAuthn Relying Party ID (domain without protocol)
RP_ID=localhost

# WebAuthn allowed origins (comma-separated)
RP_ORIGINS=http://localhost:3000,http://localhost

# JWT Secret Key (CHANGE IN PRODUCTION!)
SECRET_KEY=your-secret-key-change-in-production

# React API URL (URL where frontend can reach the backend)
# For local Docker: http://localhost:8091
# For LAN access: http://192.168.1.x:8091
REACT_APP_API_URL=http://localhost:8091
